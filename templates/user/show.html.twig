{% extends 'base.html.twig' %}

{% block title %}Fiche d'un utilisateur{% endblock %}

{% block body %}
    <nav><a href="{{ path('user') }}">Retour à l'index</a></nav>
    <h1>{{ user.firstname }} {{ user.lastname }}</h1>
    
    <h2> Roles</h2>
    <ul>
    {% for userRole in user.roles  %}
        <li>{{ userRole.role }}</li>
    {% endfor %}
    </ul>
	
	
	
	<h2>Nombres de places</h2>
	<ul>
    {% for userRepresentation in user.reservations  %}
        <li>{{ userRepresentation.places }}</li>
    {% endfor %}
    </ul>
{% endblock %}

